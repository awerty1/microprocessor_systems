/*
Задание 4. Зашифровать слово «текст» на одном светодиоде с помощью 
азбуки Морзе. Длину сигнала выбираете самостоятельно.
*/

void setup() {
	// put your setup code here, to run once:
	pinMode(OUT1, OUTPUT);

	led(OUT1, HIGH);
	delay(1000);
	led(OUT1, LOW);
	delay(250);

	led(OUT1, HIGH);
	delay(500);
	led(OUT1, LOW);
	delay(250);

	led(OUT1, HIGH);
	delay(1000);
	led(OUT1, LOW);
	delay(250);

	led(OUT1, HIGH);
	delay(500);
	led(OUT1, LOW);
	delay(250);

	led(OUT1, HIGH);
	delay(1000);
	led(OUT1, LOW);
	delay(250);

	led(OUT1, HIGH);
	delay(500);
	led(OUT1, LOW);
	delay(250);

	led(OUT1, HIGH);
	delay(500);
	led(OUT1, LOW);
	delay(250);

	led(OUT1, HIGH);
	delay(500);
	led(OUT1, LOW);
	delay(250);

	led(OUT1, HIGH);
	delay(1000);
	led(OUT1, LOW);
	delay(250);
}

void loop() {
	// put your main code here, to run repeatedly:



}